import DesignationHistory from '../../../component/DesignationHistory';

const DesignationHistoryPage = () => {
  return (
    <div>
      <DesignationHistory />
    </div>
  );
};

export default DesignationHistoryPage;
